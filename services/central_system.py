"""
"""
import time
class CentralSystem:
    def __init__(self):
        waiting  = [v for v in vehicles if v.state == 'waiting']
        for v in waiting:


        entry = {
            'action':    action,
            'reason':    reason,
        }
